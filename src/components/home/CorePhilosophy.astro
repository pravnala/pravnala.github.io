---
import data from '../../data/home/corePhilosophy.json';

const { eyebrow, heading, description, quotes, values } = data;
const headingLines = heading.split('\n');
---

<section
  class="px-6 md:px-16 py-section bg-ink-50 text-ink-900"
  aria-labelledby="philosophy-heading"
>
  <p class="reveal text-eyebrow">{eyebrow}</p>
  <div class="reveal grid grid-cols-1 md:grid-cols-[250px_1fr] gap-0 md:gap-10">
    <div class="grid md:block grid-cols-2 md:grid-cols-1">
      <h2 id="philosophy-heading" class="text-heading mb-4">
        {
          headingLines.map((line, i) => (
            <>
              {i > 0 && <br />}
              {line}
            </>
          ))
        }
      </h2>
      <p class="text-ink-500">{description}</p>
    </div>

    <dl
      class="grid grid-cols-1 md:grid-cols-2 gap-8 border-3 border-ink-400/30 p-8 md:p-10 bg-ink-600/30 text-ink-900"
    >
      {
        quotes.map(({ text, attribution }) => (
          <div>
            <p class="text-lg mb-1">"{text}"</p>
            <p class="text-xs text-ink-600 italic">{attribution}</p>
          </div>
        ))
      }
      {
        values.map(({ title, body }) => (
          <div>
            <dt class="text-2xl font-black text-ink-800 tracking-[-0.03em] leading-[1.05] mb-3">
              {title}
            </dt>
            <dd class="text-sm leading-relaxed opacity-60 max-w-sm">{body}</dd>
          </div>
        ))
      }
    </dl>
  </div>
</section>
