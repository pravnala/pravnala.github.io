---
// Keystatic admin UI — only active in dev mode.
// The @keystatic/astro integration (conditionally loaded in astro.config.mjs)
// handles rendering at runtime when prerender = false.
export const prerender = false;
---
