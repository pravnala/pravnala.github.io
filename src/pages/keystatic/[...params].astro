---
// Keystatic admin UI — only active in dev mode.
// In production this generates no pages via empty getStaticPaths.
export const prerender = true;

export function getStaticPaths() {
  return [];
}
---
